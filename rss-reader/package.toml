schema_version = 2

[identity]
name = "rss-reader"
version = "0.1.0"
display_name = "RSS 阅读器"
description = "订阅、抓取、AI 摘要的 RSS 阅读器。"

[package]
kind = "product"

[capability]
provides = ["rss.reader"]

[requires]
capabilities = ["rss.reader"]

[bindings."rss.reader"]
provider = "rss-reader"
mutable = false

[features]
default_enabled = []
optional = []
