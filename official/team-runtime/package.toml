[identity]
name = "team-runtime"
version = "0.1.0"
description = "Foundation package for devteam runtime metadata, canonical role catalog, shared context projection contracts, and delegate routing metadata."

[package]
entry = "package.wasm"
runtime = "wasm"
api_version = "v1"
provides = ["team.runtime", "team.role.catalog", "team.context.shared"]

[capability]
provides = ["team.runtime", "team.role.catalog", "team.context.shared"]
