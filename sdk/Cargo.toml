[package]
name = "weft-package-sdk"
version = "0.1.0"
edition = "2021"
description = "Shared SDK for WEFT WASM packages"

[lib]
crate-type = ["lib"]

[dependencies]
base64 = "0.22"
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

